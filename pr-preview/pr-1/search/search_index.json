{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"earthaccess-auth","text":"<p>A minimal-dependency distribution containing only the NASA Earthdata Login (EDL) authentication core of earthaccess: login strategies, token lifecycle, per-DAAC S3 credential exchange, and the redirect-safe requests session. Integrations with fsspec and obstore are optional extras, so auth-only consumers don't install the rest of earthaccess's search/download stack.</p> <p>If you only need a bearer token or temporary S3 credentials, not CMR search, this package is probably all you need.</p>"},{"location":"#install","title":"Install","text":"<pre><code>pip install earthaccess-auth            # requests + tinynetrc + typing_extensions only\npip install earthaccess-auth[fsspec]    # + fsspec/aiohttp HTTPS session\npip install earthaccess-auth[obstore]   # + obstore credential provider bridge\n</code></pre>"},{"location":"#quickstart","title":"Quickstart","text":"<pre><code>import earthaccess_auth\n\nauth = earthaccess_auth.login()  # tries env vars, then ~/.netrc, then prompts\ntoken = auth.token[\"access_token\"]\n</code></pre> <p><code>login()</code> returns an <code>Auth</code> instance rather than a module-level singleton, so you can hold onto (or pass around) multiple authenticated sessions if you need to.</p>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>Read a dataset with xarray: obstore (S3) and obspec-utils (HTTPS), side by side</li> <li>List the contents of an S3 bucket</li> <li>Identify a file from its magic bytes</li> <li>Get S3 credentials and bearer tokens</li> <li>Choosing a backend: obstore vs. obspec-utils vs. s3fs</li> <li>API reference</li> </ul>"},{"location":"explanation/choosing-a-backend/","title":"Choosing a backend","text":"<p>If you're running in AWS <code>us-west-2</code> and the data is cloud-hosted, go straight to S3 with <code>obstore</code>: fastest reads, automatic credential refresh.</p> <p>If you're running anywhere else, or the collection is on-prem only, use <code>obspec_utils.stores.AiohttpStore</code> with the headers dict from <code>http_client_options</code>. Cross-region S3 reads are billed to the requester and slower than same-region reads.</p> <p>Already on <code>s3fs</code>? <code>get_s3_credentials()</code> returns a plain temporary AWS credential dict that <code>s3fs.S3FileSystem</code> (like anything else <code>boto3</code>-shaped) accepts directly \u2014 no separate adapter needed, just no automatic refresh: re-call <code>get_s3_credentials()</code> yourself once credentials near expiry.</p> <p>If you just need a token or headers and no file access at all, skip both. See Get S3 credentials and bearer tokens.</p>"},{"location":"howto/list-bucket-contents/","title":"List the contents of an S3 bucket","text":"<p>Useful for exploring what a DAAC's cloud bucket actually contains before you build a granule URL by hand, or for a quick sanity check that your S3 credentials work at all.</p> <pre><code># /// script\n# requires-python = \"&gt;=3.11\"\n# dependencies = [\"earthaccess-auth[obstore]\"]\n#\n# [tool.uv.sources]\n# # TODO: switch to a released earthaccess-auth once this branch merges\n# earthaccess-auth = { git = \"https://github.com/maxrjones/earthaccess\", subdirectory = \"earthaccess-auth\", branch = \"poc/earthaccess-auth\" }\n# ///\n\"\"\"List the contents of an S3 bucket prefix.\n\nUseful for exploring what a DAAC's cloud bucket actually contains before\nbuilding a granule URL by hand, or for a quick sanity check that S3\ncredentials work at all.\n\"\"\"\n\nimport obstore\nfrom obstore.store import S3Store\n\nimport earthaccess_auth\nfrom earthaccess_auth.adapters.obstore import s3_credential_provider\n\nauth = earthaccess_auth.login()\n\ncredential_provider = s3_credential_provider(\n    auth,\n    credentials_endpoint=\"https://data.nsidc.earthdatacloud.nasa.gov/s3credentials\",\n)\nstore = S3Store(\n    \"nsidc-cumulus-prod-protected\",\n    region=\"us-west-2\",\n    credential_provider=credential_provider,\n)\n\nfor obj in obstore.list(store, prefix=\"ATLAS/ATL03/006/2020/01/01\"):\n    print(obj[\"path\"], obj[\"size\"])\n</code></pre> <p><code>obstore.list()</code> returns a lazily-paginated stream of <code>ObjectMeta</code> dicts (<code>path</code>, <code>size</code>, <code>last_modified</code>, <code>e_tag</code>). Iterating it fetches pages as needed instead of loading the whole prefix into memory up front.</p> <p>Already on <code>s3fs</code>? <code>auth.get_s3_credentials(daac=...)</code> returns a plain temporary AWS credential dict that <code>s3fs.S3FileSystem</code> (like anything else <code>boto3</code>-shaped) accepts directly \u2014 no separate adapter needed. See Get S3 credentials and bearer tokens.</p> <p>This script is <code>examples/list_bucket_contents.py</code>, and declares its own dependencies (PEP 723), so <code>uv run examples/list_bucket_contents.py</code> works standalone.</p>"},{"location":"howto/magic-bytes/","title":"Identify a file from its magic bytes","text":"<p>NASA Earthdata granule filenames don't always advertise their format reliably, and you don't want to download a multi-gigabyte file just to find out it's HDF5 instead of NetCDF classic. A byte-range request fetches just the first few bytes, so you can check them against known file signatures without downloading the whole object:</p> Format Magic bytes (hex) Magic bytes (ASCII) HDF5 (and NetCDF-4, which is HDF5-based) <code>89 48 44 46 0d 0a 1a 0a</code> <code>\\x89HDF\\r\\n\\x1a\\n</code> NetCDF classic (CDF-1/2) <code>43 44 46 01</code> / <code>43 44 46 02</code> <code>CDF\\x01</code> / <code>CDF\\x02</code> NetCDF classic (CDF-5, 64-bit) <code>43 44 46 05</code> <code>CDF\\x05</code> GeoTIFF (little-endian) <code>49 49 2a 00</code> <code>II*\\x00</code> GeoTIFF (big-endian) <code>4d 4d 00 2a</code> <code>MM\\x00*</code> <p>Zarr has no comparable magic bytes. A Zarr store is a directory (or prefix) of many small objects (<code>zarr.json</code>/<code>.zarray</code>, <code>.zmetadata</code>, chunk files), not a single self-describing binary blob. \"Is this Zarr?\" means checking whether a <code>zarr.json</code> or <code>.zarray</code> key exists at that prefix, not sniffing bytes.</p> <pre><code># /// script\n# requires-python = \"&gt;=3.11\"\n# dependencies = [\"earthaccess-auth[obstore]\"]\n#\n# [tool.uv.sources]\n# # TODO: switch to a released earthaccess-auth once this branch merges\n# earthaccess-auth = { git = \"https://github.com/maxrjones/earthaccess\", subdirectory = \"earthaccess-auth\", branch = \"poc/earthaccess-auth\" }\n# ///\n\"\"\"Identify a granule's format from its magic bytes, without downloading it.\n\nA byte-range request pulls just the first few bytes, cheap even for a\nmulti-gigabyte file.\n\"\"\"\n\nfrom obstore.store import S3Store\n\nimport earthaccess_auth\nfrom earthaccess_auth.adapters.obstore import s3_credential_provider\n\nauth = earthaccess_auth.login()\n\ncredential_provider = s3_credential_provider(\n    auth,\n    credentials_endpoint=\"https://data.nsidc.earthdatacloud.nasa.gov/s3credentials\",\n)\nstore = S3Store(\n    \"nsidc-cumulus-prod-protected\",\n    region=\"us-west-2\",\n    credential_provider=credential_provider,\n)\n\npath = \"ATLAS/ATL03/006/2020/01/01/ATL03_20200101T000106_00650601_006_01.h5\"\nheader = bytes(store.get_range(path, start=0, end=8))\n\nif header == b\"\\x89HDF\\r\\n\\x1a\\n\":\n    print(\"HDF5 (or NetCDF-4)\")\nelif header[:3] == b\"CDF\":\n    print(f\"NetCDF classic, version {header[3]}\")\nelse:\n    print(f\"unrecognized: {header!r}\")\n</code></pre> <p>This script is <code>examples/magic_bytes.py</code>, and declares its own dependencies (PEP 723), so <code>uv run examples/magic_bytes.py</code> works standalone.</p>"},{"location":"howto/read-a-dataset/","title":"Read a dataset with xarray","text":"<p>Every NASA Earthdata granule is served from one of two places: a plain HTTPS URL (on-prem DAAC archives), or an S3 bucket in <code>us-west-2</code> (NASA Earthdata Cloud, for clients running in that same region). Once you're authenticated, <code>earthaccess-auth</code> gets you a credential provider or a headers dict for either, and obspec-utils's <code>EagerStoreReader</code> wraps either backend with the same file-like interface, so the <code>xarray.open_dataset</code> call underneath doesn't change between them.</p> obstore (S3)obspec-utils (HTTPS) <p>Requires the <code>obstore</code> extra plus <code>obspec-utils</code>: <code>pip install earthaccess-auth[obstore] obspec-utils</code>.</p> <pre><code># /// script\n# requires-python = \"&gt;=3.11\"\n# dependencies = [\n#     \"earthaccess-auth[obstore]\",\n#     \"obspec-utils\",\n#     \"xarray\",\n#     \"h5netcdf\",\n# ]\n#\n# [tool.uv.sources]\n# # TODO: switch to a released earthaccess-auth once this branch merges\n# earthaccess-auth = { git = \"https://github.com/maxrjones/earthaccess\", subdirectory = \"earthaccess-auth\", branch = \"poc/earthaccess-auth\" }\n# ///\n\"\"\"Read an S3-hosted granule into xarray via obstore + obspec-utils.\n\nFastest path for data in NASA's Earthdata Cloud, when running inside AWS\nus-west-2 (same-region S3 reads avoid cross-region egress).\n\"\"\"\n\nimport xarray as xr\nfrom obspec_utils.readers import EagerStoreReader\nfrom obstore.store import S3Store\n\nimport earthaccess_auth\nfrom earthaccess_auth.adapters.obstore import s3_credential_provider\n\nauth = earthaccess_auth.login()\n\ncredential_provider = s3_credential_provider(\n    auth,\n    credentials_endpoint=\"https://data.nsidc.earthdatacloud.nasa.gov/s3credentials\",\n)\nstore = S3Store(\n    \"nsidc-cumulus-prod-protected\",\n    region=\"us-west-2\",\n    credential_provider=credential_provider,\n)\n\npath = \"ATLAS/ATL03/006/2020/01/01/ATL03_20200101T000106_00650601_006_01.h5\"\nreader = EagerStoreReader(store, path)\nds = xr.open_dataset(reader, engine=\"h5netcdf\")\nprint(ds)\n</code></pre> <p>Requires the <code>obstore</code> extra (for <code>http_client_options</code>) plus <code>obspec-utils</code> and <code>aiohttp</code>: <code>pip install earthaccess-auth[obstore] obspec-utils aiohttp</code>.</p> <p>Use this when the granule is only available on-prem (no S3 bucket), or when you're running outside <code>us-west-2</code> and don't want to pay cross-region S3 egress.</p> <pre><code># /// script\n# requires-python = \"&gt;=3.11\"\n# dependencies = [\n#     \"earthaccess-auth[obstore]\",\n#     \"obspec-utils\",\n#     \"aiohttp\",\n#     \"xarray\",\n#     \"h5netcdf\",\n# ]\n#\n# [tool.uv.sources]\n# # TODO: switch to a released earthaccess-auth once this branch merges\n# earthaccess-auth = { git = \"https://github.com/maxrjones/earthaccess\", subdirectory = \"earthaccess-auth\", branch = \"poc/earthaccess-auth\" }\n# ///\n\"\"\"Read an on-prem (non-S3) granule into xarray over HTTPS.\n\nUse this when the data isn't in NASA's Earthdata Cloud, or when running\noutside us-west-2 and cross-region S3 egress isn't worth paying for. Uses\nthe same EagerStoreReader as the S3 example: xarray doesn't need to know\nwhich backend is underneath.\n\"\"\"\n\nimport xarray as xr\nfrom obspec_utils.readers import EagerStoreReader\nfrom obspec_utils.stores import AiohttpStore\n\nimport earthaccess_auth\nfrom earthaccess_auth.adapters.obstore import http_client_options\n\nauth = earthaccess_auth.login()\nheaders = http_client_options(auth)[\"default_headers\"]\n\nurl = \"https://daac.ornl.gov/daacdata/npp/grassland/NPP_BCN/data/example.nc\"\nbase_url, path = url.rsplit(\"/\", 1)\n\nstore = AiohttpStore(base_url, headers=headers)\nreader = EagerStoreReader(store, path)\nds = xr.open_dataset(reader, engine=\"h5netcdf\")\nprint(ds)\n</code></pre> <p>Both scripts live in <code>examples/</code> and declare their own dependencies (PEP 723), so <code>uv run examples/read_a_dataset_s3.py</code> works standalone, no separate install step required.</p> <p>S3 credentials are DAAC-scoped and short-lived</p> <p><code>get_s3_credentials()</code> / <code>s3_credential_provider()</code> return credentials valid for one DAAC's cloud bucket(s), for about an hour. If you're reading granules from more than one DAAC, fetch credentials per DAAC; if a read fails with an auth error partway through a long-running job, get fresh credentials rather than retrying with the ones you have.</p> <p>See Choosing a backend for how to decide between these two.</p>"},{"location":"howto/s3-credentials-and-bearer-token/","title":"Get S3 credentials and bearer tokens","text":"<p><code>earthaccess-auth</code> gives you three things once you're logged in: a bearer token, temporary AWS S3 credentials, and (via the <code>obstore</code> extra) a credential provider that fetches and refreshes those credentials for you. Which one you want depends on what you're handing it to. Each of these is also a standalone, runnable script in <code>examples/</code> declaring its own dependencies (PEP 723).</p>"},{"location":"howto/s3-credentials-and-bearer-token/#the-raw-bearer-token","title":"The raw bearer token","text":"<p>The minimal case: no fsspec, no obstore, just the token string. Inject it into a header yourself. This is the pattern a Lambda-deployed consumer with a tight dependency budget would use.</p> <pre><code># /// script\n# requires-python = \"&gt;=3.11\"\n# dependencies = [\"earthaccess-auth\"]\n#\n# [tool.uv.sources]\n# # TODO: switch to a released earthaccess-auth once this branch merges\n# earthaccess-auth = { git = \"https://github.com/maxrjones/earthaccess\", subdirectory = \"earthaccess-auth\", branch = \"poc/earthaccess-auth\" }\n# ///\n\"\"\"Get a raw EDL bearer token.\n\nThe minimal case: no fsspec, no obstore, just the token string. This is the\npattern a Lambda-deployed consumer with a tight dependency budget would use.\n\"\"\"\n\nimport earthaccess_auth\n\nauth = earthaccess_auth.login(strategy=\"environment\")\ntoken = auth.token[\"access_token\"]\nprint(token)\n</code></pre>"},{"location":"howto/s3-credentials-and-bearer-token/#header-dict-for-http-based-stores","title":"Header dict for HTTP-based stores","text":"<p>For anything that accepts a plain <code>headers</code> dict (obstore HTTP stores, obspec-utils's <code>AiohttpStore</code> \u2014 see Read a dataset with xarray \u2014 or icechunk's <code>http_store(headers=...)</code> for virtual chunk containers) use <code>http_client_options</code>:</p> <pre><code># /// script\n# requires-python = \"&gt;=3.11\"\n# dependencies = [\"earthaccess-auth[obstore]\"]\n#\n# [tool.uv.sources]\n# # TODO: switch to a released earthaccess-auth once this branch merges\n# earthaccess-auth = { git = \"https://github.com/maxrjones/earthaccess\", subdirectory = \"earthaccess-auth\", branch = \"poc/earthaccess-auth\" }\n# ///\n\"\"\"Build a headers dict for HTTP-based stores fronting EDL-protected data.\n\nWorks for obstore HTTP stores and icechunk's http_store(headers=...) for\nvirtual chunk containers.\n\"\"\"\n\nimport earthaccess_auth\nfrom earthaccess_auth.adapters.obstore import http_client_options\n\nauth = earthaccess_auth.login()\noptions = http_client_options(auth)\nprint(options)  # default_headers: {\"authorization\": \"Bearer &lt;token&gt;\"}\n</code></pre>"},{"location":"howto/s3-credentials-and-bearer-token/#temporary-aws-s3-credentials","title":"Temporary AWS S3 credentials","text":"<p>For anything that wants <code>key</code>/<code>secret</code>/<code>token</code> (or <code>aws_access_key_id</code> /<code>aws_secret_access_key</code>/<code>aws_session_token</code>) directly, such as <code>boto3</code>, <code>s3fs</code>, or the AWS CLI:</p> <pre><code># /// script\n# requires-python = \"&gt;=3.11\"\n# dependencies = [\"earthaccess-auth\"]\n#\n# [tool.uv.sources]\n# # TODO: switch to a released earthaccess-auth once this branch merges\n# earthaccess-auth = { git = \"https://github.com/maxrjones/earthaccess\", subdirectory = \"earthaccess-auth\", branch = \"poc/earthaccess-auth\" }\n# ///\n\"\"\"Get temporary AWS S3 credentials for a DAAC.\n\nFor anything that wants key/secret/token directly: boto3, s3fs, or the AWS\nCLI. Credentials are scoped to that DAAC's cloud bucket(s) and expire in\nabout an hour.\n\"\"\"\n\nimport earthaccess_auth\n\nauth = earthaccess_auth.login()\ncreds = auth.get_s3_credentials(daac=\"NSIDC\")\nprint(creds)  # accessKeyId, secretAccessKey, sessionToken, expiration\n</code></pre> <p>You can look credentials up by DAAC short name (<code>daac=\"NSIDC\"</code>), by cloud provider code (<code>provider=\"NSIDC_CPRD\"</code>, from <code>find_provider</code>), or by a raw <code>s3credentials</code> endpoint URL (<code>endpoint=...</code>) if you already have one. These credentials are scoped to that DAAC's cloud bucket(s) and expire in about an hour, so don't cache them longer than that.</p>"},{"location":"howto/s3-credentials-and-bearer-token/#an-obstore-credential-provider","title":"An obstore credential provider","text":"<p>If you're handing credentials to an <code>obstore.store.S3Store</code> (see Read a dataset with xarray), pass a provider instead of a one-shot credentials dict. The provider re-calls <code>get_s3_credentials()</code> on its own once the current credentials near expiry, so a long-running job doesn't need its own refresh loop:</p> <pre><code># /// script\n# requires-python = \"&gt;=3.11\"\n# dependencies = [\"earthaccess-auth[obstore]\"]\n#\n# [tool.uv.sources]\n# # TODO: switch to a released earthaccess-auth once this branch merges\n# earthaccess-auth = { git = \"https://github.com/maxrjones/earthaccess\", subdirectory = \"earthaccess-auth\", branch = \"poc/earthaccess-auth\" }\n# ///\n\"\"\"Build an obstore credential provider that refreshes S3 credentials itself.\n\nHand this to obstore.store.S3Store (or obspec_utils' readers, see\nread_a_dataset_s3.py) instead of a one-shot credentials dict, so a\nlong-running job doesn't need its own refresh loop.\n\"\"\"\n\nimport earthaccess_auth\nfrom earthaccess_auth.adapters.obstore import s3_credential_provider\n\nauth = earthaccess_auth.login()\n\ncredential_provider = s3_credential_provider(\n    auth,\n    credentials_endpoint=\"https://data.nsidc.earthdatacloud.nasa.gov/s3credentials\",\n)\nprint(credential_provider)\n</code></pre> <p><code>credentials_endpoint</code> is a DAAC's <code>s3credentials</code> URL: the <code>\"s3-credentials\"</code> field on each entry in <code>DAACS</code>.</p>"},{"location":"reference/api/","title":"API reference","text":""},{"location":"reference/api/#core","title":"Core","text":""},{"location":"reference/api/#earthaccess_auth.login","title":"<code>earthaccess_auth.login(strategy='all', persist=False, system=PROD)</code>","text":"<p>Authenticate with Earthdata Login (EDL) and return an Auth instance.</p> <p>Parameters:</p> Name Type Description Default <code>strategy</code> <code>str</code> <p>The authentication method.</p> <ul> <li>\"all\": (default) Try, in order: environment variables,     <code>~/.netrc</code>, then an interactive prompt \u2014 stopping at the     first one that works.</li> <li>\"interactive\": Enter a username and password.</li> <li>\"netrc\": Retrieve a username and password from <code>~/.netrc</code>.</li> <li>\"environment\":     Retrieve either a username and password pair from the     <code>EARTHDATA_USERNAME</code> and <code>EARTHDATA_PASSWORD</code> environment     variables, or an Earthdata login token from the     <code>EARTHDATA_TOKEN</code> environment variable.</li> </ul> <code>'all'</code> <code>persist</code> <code>bool</code> <p>Persist username and password credentials in a <code>.netrc</code> file.</p> <code>False</code> <code>system</code> <code>System</code> <p>The EDL endpoint to authenticate against. Defaults to <code>PROD</code>.</p> <code>PROD</code> <p>Returns:</p> Type Description <code>Auth</code> <p>An authenticated <code>Auth</code> instance. Hold onto it yourself \u2014 there's no</p> <code>Auth</code> <p>module-level singleton, so pass it to whatever needs it.</p>"},{"location":"reference/api/#earthaccess_auth.Auth","title":"<code>earthaccess_auth.Auth(user_agent=None)</code>","text":"<p>Authentication class for operations that require Earthdata login (EDL).</p>"},{"location":"reference/api/#earthaccess_auth.Auth.get_s3_credentials","title":"<code>get_s3_credentials(daac=None, provider=None, endpoint=None)</code>","text":"<p>Get temporary AWS S3 credentials for a NASA DAAC's cloud bucket(s).</p> <p>Usually you only need <code>daac</code>. <code>provider</code> and <code>endpoint</code> are for when you already know the DAAC's cloud provider code or its <code>s3credentials</code> URL and want to skip the DAAC registry lookup.</p> <p>Parameters:</p> Name Type Description Default <code>daac</code> <code>str | None</code> <p>A DAAC's short name, e.g. <code>\"NSIDC\"</code> or <code>\"PODAAC\"</code>.</p> <code>None</code> <code>provider</code> <code>str | None</code> <p>A DAAC's cloud provider code, e.g. <code>\"NSIDC_CPRD\"</code>.</p> <code>None</code> <code>endpoint</code> <code>str | None</code> <p>A DAAC's <code>s3credentials</code> URL directly.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>A dict with the temporary AWS S3 credentials (<code>accessKeyId</code>,</p> <code>dict[str, str]</code> <p><code>secretAccessKey</code>, <code>sessionToken</code>, <code>expiration</code>), or an empty</p> <code>dict[str, str]</code> <p>dict if not authenticated yet or the provider has no S3</p> <code>dict[str, str]</code> <p>credentials available.</p>"},{"location":"reference/api/#earthaccess_auth.Auth.get_session","title":"<code>get_session()</code>","text":"<p>Build a new <code>requests.Session</code> with EDL authentication configured.</p> <p>Returns:</p> Type Description <code>Session</code> <p>A <code>requests.Session</code> carrying the bearer token (if authenticated)</p> <code>Session</code> <p>and re-attaching EDL credentials after redirects.</p>"},{"location":"reference/api/#earthaccess_auth.Auth.login","title":"<code>login(strategy='netrc', persist=False, system=None)</code>","text":"<p>Authenticate with Earthdata Login (EDL).</p> <p>Parameters:</p> Name Type Description Default <code>strategy</code> <code>str</code> <p>The authentication method.</p> <ul> <li>\"interactive\": Enter a username and password.</li> <li>\"netrc\": (default) Retrieve a username and password from <code>~/.netrc</code>.</li> <li>\"environment\":     Retrieve either a username and password pair from the     <code>EARTHDATA_USERNAME</code> and <code>EARTHDATA_PASSWORD</code> environment variables,     or an Earthdata login token from the <code>EARTHDATA_TOKEN</code> environment     variable.</li> </ul> <code>'netrc'</code> <code>persist</code> <code>bool</code> <p>Persist username and password credentials in a <code>.netrc</code> file.</p> <code>False</code> <code>system</code> <code>System | None</code> <p>The EDL endpoint to authenticate against. Defaults to <code>PROD</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>This <code>Auth</code> instance, now authenticated.</p> <p>Raises:</p> Type Description <code>LoginAttemptFailure</code> <p>If the NASA Earthdata Login service rejects credentials.</p>"},{"location":"reference/api/#earthaccess_auth.Auth.refresh_tokens","title":"<code>refresh_tokens()</code>","text":"<p>Refresh CMR tokens.</p> <p>CMR tokens authenticate queries for restricted and early-access datasets. This renews them so queries keep working for whatever collections the authenticated user has access to.</p>"},{"location":"reference/api/#daac-registry","title":"DAAC registry","text":""},{"location":"reference/api/#earthaccess_auth.daac.DAACS","title":"<code>earthaccess_auth.daac.DAACS</code>  <code>module-attribute</code>","text":"<p>The registry of NASA Earthdata DAACs known to <code>earthaccess-auth</code>.</p> <p>Each entry is a <code>DAACConfig</code> describing one DAAC's short name, cloud/on-prem provider codes, and <code>s3credentials</code> endpoint. Look entries up with <code>find_provider</code> rather than scanning this list directly.</p>"},{"location":"reference/api/#earthaccess_auth.daac.find_provider","title":"<code>earthaccess_auth.daac.find_provider(daac_short_name=None, cloud_hosted=None)</code>","text":"<p>Look up a DAAC's CMR provider code by its short name.</p> <p>Parameters:</p> Name Type Description Default <code>daac_short_name</code> <code>str | None</code> <p>A DAAC's short name, e.g. <code>\"NSIDC\"</code> or <code>\"PODAAC\"</code>.</p> <code>None</code> <code>cloud_hosted</code> <code>bool | None</code> <p>If <code>True</code>, prefer the DAAC's cloud provider code over its on-prem one, falling back to on-prem if the DAAC has no cloud provider.</p> <code>None</code> <p>Returns:</p> Type Description <code>str | None</code> <p>The matching provider code (e.g. <code>\"NSIDC_CPRD\"</code>), or <code>None</code> if</p> <code>str | None</code> <p><code>daac_short_name</code> isn't in the</p> <code>str | None</code> <p>DAAC registry.</p>"},{"location":"reference/api/#earthaccess_auth.daac.find_provider_by_shortname","title":"<code>earthaccess_auth.daac.find_provider_by_shortname(short_name, cloud_hosted)</code>","text":"<p>Look up a collection's CMR provider code by querying CMR directly.</p> <p>Unlike <code>find_provider</code>, this queries CMR itself instead of the local DAAC registry, so it also works for collections whose provider isn't listed in <code>DAACS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>short_name</code> <code>str</code> <p>A collection's short name, e.g. <code>\"ATL03\"</code>.</p> required <code>cloud_hosted</code> <code>bool</code> <p>Whether to search for the cloud-hosted or on-prem version of the collection.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>The provider ID of the first matching collection, or <code>None</code> if no</p> <code>str | None</code> <p>collection with that short name was found.</p>"},{"location":"reference/api/#systems","title":"Systems","text":""},{"location":"reference/api/#earthaccess_auth.System","title":"<code>earthaccess_auth.System(cmr_base_url, status_url, status_api_url, edl_hostname)</code>  <code>dataclass</code>","text":"<p>Host URL options, for different Earthdata domains.</p>"},{"location":"reference/api/#earthaccess_auth.PROD","title":"<code>earthaccess_auth.PROD = System(CMRBaseURL('https://cmr.earthdata.nasa.gov/search/'), StatusURL('https://status.earthdata.nasa.gov/'), StatusApiURL('https://status.earthdata.nasa.gov/api/v1/statuses'), EDLHostname('urs.earthdata.nasa.gov'))</code>  <code>module-attribute</code>","text":"<p>NASA's production Earthdata system. The default for <code>login(system=...)</code>.</p>"},{"location":"reference/api/#earthaccess_auth.UAT","title":"<code>earthaccess_auth.UAT = System(CMRBaseURL('https://cmr.uat.earthdata.nasa.gov/search/'), StatusURL('https://status.uat.earthdata.nasa.gov/'), StatusApiURL('https://status.uat.earthdata.nasa.gov/api/v1/statuses'), EDLHostname('uat.urs.earthdata.nasa.gov'))</code>  <code>module-attribute</code>","text":"<p>NASA's user acceptance testing (UAT) Earthdata system, for pre-release testing.</p>"},{"location":"reference/api/#exceptions","title":"Exceptions","text":""},{"location":"reference/api/#earthaccess_auth.LoginStrategyUnavailable","title":"<code>earthaccess_auth.LoginStrategyUnavailable</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a login strategy couldn't be attempted at all.</p> <p>For example, the <code>\"environment\"</code> strategy raises this if none of <code>EARTHDATA_USERNAME</code>/<code>EARTHDATA_PASSWORD</code>/<code>EARTHDATA_TOKEN</code> are set. Contrast with <code>LoginAttemptFailure</code>, which is raised when a strategy was attempted but Earthdata Login rejected the credentials.</p>"},{"location":"reference/api/#earthaccess_auth.LoginAttemptFailure","title":"<code>earthaccess_auth.LoginAttemptFailure</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when Earthdata Login rejects an authentication attempt.</p> <p>For example, because the supplied username/password or token were invalid. Contrast with <code>LoginStrategyUnavailable</code>, which is raised when a strategy couldn't even be attempted (e.g. missing credentials).</p>"},{"location":"reference/api/#adapters","title":"Adapters","text":""},{"location":"reference/api/#fsspec-extra-earthaccess-authfsspec","title":"fsspec (extra: <code>earthaccess-auth[fsspec]</code>)","text":""},{"location":"reference/api/#earthaccess_auth.adapters.fsspec.get_fsspec_https_session","title":"<code>earthaccess_auth.adapters.fsspec.get_fsspec_https_session(auth)</code>","text":"<p>Build an fsspec HTTPFileSystem that sends the EDL bearer token on every request.</p> <p><code>trust_env</code> is disabled: if aiohttp also picked up ambient credentials from the environment while a bearer token is set, EDL would reject the request.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>Auth</code> <p>An authenticated <code>Auth</code> instance.</p> required <p>Returns:</p> Type Description <code>AbstractFileSystem</code> <p>An <code>fsspec.AbstractFileSystem</code> (<code>HTTPFileSystem</code>) ready to pass to</p> <code>AbstractFileSystem</code> <p><code>xarray.open_dataset</code> or open files directly.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>auth</code> has not been authenticated (<code>auth.token is None</code>).</p>"},{"location":"reference/api/#obstore-extra-earthaccess-authobstore","title":"obstore (extra: <code>earthaccess-auth[obstore]</code>)","text":""},{"location":"reference/api/#earthaccess_auth.adapters.obstore.s3_credential_provider","title":"<code>earthaccess_auth.adapters.obstore.s3_credential_provider(auth, credentials_endpoint)</code>","text":"<p>Build an obstore credential provider that refreshes EDL-issued S3 credentials.</p> <p>Hand this to <code>obstore.store.S3Store(credential_provider=...)</code> instead of a one-shot credentials dict, so a long-running job doesn't need its own refresh loop \u2014 the provider re-authenticates with EDL once the current credentials near expiry.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>Auth</code> <p>An authenticated <code>Auth</code> instance.</p> required <code>credentials_endpoint</code> <code>str</code> <p>A DAAC's <code>s3credentials</code> URL, e.g. the <code>\"s3-credentials\"</code> field on an entry in <code>DAACS</code>.</p> required <p>Returns:</p> Type Description <code>NasaEarthdataCredentialProvider</code> <p>A credential provider usable as <code>obstore.store.S3Store</code>'s</p> <code>NasaEarthdataCredentialProvider</code> <p><code>credential_provider</code> argument.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>auth</code> has not been authenticated (<code>auth.token is None</code>).</p>"},{"location":"reference/api/#earthaccess_auth.adapters.obstore.http_client_options","title":"<code>earthaccess_auth.adapters.obstore.http_client_options(auth)</code>","text":"<p>Build default-header client options for HTTPS stores fronting EDL-protected data.</p> <p>Usable for obstore HTTP stores and any store config that accepts plain headers, such as icechunk's <code>http_store(headers=...)</code> for virtual chunk containers.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>Auth</code> <p>An authenticated <code>Auth</code> instance.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dict with a <code>default_headers</code> key carrying the bearer token,</p> <code>dict[str, Any]</code> <p>matching <code>obstore.store.ClientConfig</code>'s shape.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>auth</code> has not been authenticated (<code>auth.token is None</code>).</p>"}]}